<div class="container">
    <h3><%= @post.title %></h3>
    
        Submitted <%= time_ago_in_words(@post.created_at) %> ago. | <%= link_to 'Edit', edit_post_path(@post) %> | <%= link_to 'Delete', post_path(@post), method: :delete %>
        <div class="well well-sm">
        <p>
            <%= @post.body %>
        </p>
        </div>
</div>